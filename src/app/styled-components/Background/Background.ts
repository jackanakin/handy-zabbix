import styled from "styled-components";

import { darkTheme } from 'app/themes/themes/darkTheme';

export const Background = styled.div`
    background-color: ${darkTheme.layout.pageBackground};
    height: 100vh;
`;